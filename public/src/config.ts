export const PRODUCT_DESCRIPTION_MAX_LENGTH: number = 500;
export const MAX_IMAGE_SIZE: number = 1024 * 1024 * 2;
export const MAX_IMAGE_SIZE_WHOLE_NUMBER: string = MAX_IMAGE_SIZE.toString().slice(0, 1);
export const MAX_IMAGES_NUMBER: number = 4;
